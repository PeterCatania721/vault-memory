# Archived scripts

One-shot migration tools kept for reference. Not used in v0.2.4+ normal operation.

| Script | Purpose |
|--------|---------|
| `migrate-qdrant-to-neo4j.py` | v0.1.x Qdrant → Neo4j migration (requires `qdrant-client`) |

Re-index from Obsidian instead: `sync_vault force=true`.
