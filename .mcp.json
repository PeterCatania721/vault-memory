{
  "mcpServers": {
    "vault-memory": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "${GROK_PLUGIN_ROOT}/mcp-server",
        "vault-memory-mcp"
      ],
      "env": {
        "VAULT_MEMORY_CONFIG": "${HOME}/.vault-memory/config.yaml"
      }
    }
  }
}