# vault-memory MCP server

Stdio MCP server for **Obsidian + Neo4j** (graph, vector embeddings, provenance GraphRAG).

```bash
uv sync --all-extras
uv run vault-memory-mcp
```

**v0.2.4** — 25 tools. Agent: `query_agent_guidance`. General: `search_vault_hybrid`.